#include <iostream>

#include "utils.h"
#include "metro.h"

int append_station(const char *filename, const char *id, const char *name)
{
    if (has_file_in_dir(".", filename))
    {
    }
    return 0;
}